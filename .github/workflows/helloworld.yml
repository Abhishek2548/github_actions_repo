name: "hello world wprkflow"

on:
  push:
    branches:
      -master
      -production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "print hello world"
        run: echo "my name is abhishek"
